name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Install dependencies
      run: cd /usr/src
    - name: tesst 
      run: sshpass -p "Vnwd5PHHb" ssh -o StrictHostKeyChecking=no -p 9020 root@103.173.154.181 "cd /usr/src && mkdir test && exit"

    # Add a test script to composer.json, for instance: "test": "vendor/bin/phpunit"
    # Docs: https://getcomposer.org/doc/articles/scripts.md

    # - name: Run test suite
    #   run: composer run-script test
