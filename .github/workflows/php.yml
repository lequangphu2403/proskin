name: PHP Composer

stages:
  - main
  - dev

deploy_main:
  stage : main

  on:
    push:
      branches: [ "main" ]
    pull_request:
      branches: [ "main" ]

  permissions:
    contents: read

  jobs:
    build:

      runs-on: ubuntu-latest

      steps:
      - run: echo "Lê Quang Phú"
deploy_dev:
  stage : dev
  script:
    - echo "Compiling the code..."
    - echo "Compile complete."