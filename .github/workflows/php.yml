name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: ssh parallels@10.211.55.4 cd /var/www/ && git clone https://github.com/lequangphu2403/test_cicd.git
    - run: echo "Lê Quang Ph"
