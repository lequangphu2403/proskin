name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: echo "${{variables.SSH_USER}}"
    - run: echo "Phú"
    - name: connect and pull
      run: ssh -p 9020 ${{variables.SSH_USER}}@${{variables.SSH_HOST}} 'hostname && echo 'Welcome!!!' > welcome.txt'
