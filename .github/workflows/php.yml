name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: ssh -o 'StrictHostKeyChecking no' -p 9020 root@103.173.154.181 'echo 123'
    # - name: Deploy source-code
    #   uses: appleboy/ssh-action@v0.1.10
    #   env:
    #       host: 103.173.154.181
    #       username: root
    #       port: 9020
    #       key: ${{ secrets.SSH_PRIVATE_KEY }}
    #   with:
    #     host: 103.173.154.181
    #     username: root
    #     port: 9020
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}