name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: ssh-keyscan $SSH_HOST >> ~/.ssh/known_hosts
    - run: chmod 644 ~/.ssh/known_hosts
    - run: echo "$SSH_USER"
    - run: echo "Phú"
    - name: connect and pull
      run: ssh -p 9020 $SSH_USER@$SSH_HOST 'hostname && echo 'Welcome!!!' > welcome.txt'
