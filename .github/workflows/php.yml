name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: echo "${{ secrets.SSH_USER }}"
    - run: echo "Phú"
    - name: connect and pull
      run: sshpass -p 'Vnwd5PHHb' ssh -p 9020 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "cd /usr/src/ && git clone https://github.com/lequangphu2403/test_cicd.git"
    - name: show
      run: echo "Le Quang Phu 1"