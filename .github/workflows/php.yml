stages:
  - main
build-job:
  stage: main
  script:
    - echo $PRIVATE_KEY
    - echo 12311
#    # chạy ssh-agent tương ứng với Gitlab Runner hiện tại
#    - eval $(ssh-agent -s)
#
#    # thêm nội dung của biến SSH_PRIVATE_KEY vào agent store
#    - bash -c 'ssh-add <(echo "$SSH_PRIVATE_KEY")'
#
#    # tạo folder ~/.ssh
#    - mkdir -p ~/.ssh
#
#    # Scan lấy SSH Host key cho địa chỉ IP server
#    # Được kết quả bao nhiêu thì thêm vào file known_hosts
#    - ssh-keyscan -H $SSH_SERVER_IP >> ~/.ssh/known_hosts
#
#    # Sửa lại quyền của file known_hosts
#    - chmod 644 ~/.ssh/known_hosts
#    - ssh -p 9020 $SSH_USER@$SSH_SERVER_IP
#      " echo 'Vnwd5PHHb';
#      cd ${PATH_TO_PROJECT};
#      mkdir test;"
#
#
##    - ssh-keygen -Y -f $PRIVATE_KEY
##    - apk update && apk add openssh-client
##    - ssh-keyscan -H 103.173.154.181 >> ~/.ssh/known_hosts
##    - ssh -tt -o StrictHostKeyChecking=no -p 9020 root@103.173.154.181 "echo 'Vnwd5PHHb' && cd /usr/src && mkdir test && exit"
##    - echo "Compiling the code..."
##    - echo "Compile complete. 1"
##    - cd /usr/src && mkdir test.txt
